# Images

`hero-play-structure.jpg` is the client's architectural render of the play
floor, used by the homepage hero and the /early-bird pageHeader. Source:
`TPC/client files/images/Screenshot 2026-07-15 at 3.23.48 PM.png`, converted
to JPEG.

It is a render, not photography — final photography is still pending. Replace
it at this same path and nothing else changes.

The remaining image paths referenced from `/content/` have no file yet and will
show their alt text until real photography arrives.
